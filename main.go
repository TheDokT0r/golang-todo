package main

import (
	"todo/cmd"
)

func main() {
	// var vault []items.Item

	// vault = append(vault, items.Item{Name: "lol"})
	// vault = append(vault, items.Item{Name: "lol2"})
	// vault = append(vault, items.Item{Name: "lol3"})

	cmd.RenderMenu(0)
}
